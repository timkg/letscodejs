// launch server the same way we would in production
// get a page
// confirm we got something

'use strict';

exports.test_smoke_test = function(test) {
	test.ok(false, 'ok', 'smoke test');
	test.done();
};